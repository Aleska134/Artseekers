{
	"name": "Artseekers Simplified",
	"image": "mcr.microsoft.com/devcontainers/universal:2",
	"forwardPorts": [3000, 3001],
	"onCreateCommand": "cp .env.example .env || echo '.env already exists'",
	"postCreateCommand": "npm install && pipenv install",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}